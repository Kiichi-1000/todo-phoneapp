# v1.4 リリース準備 — 完了状況とハンドオフ (2026-05-21)

ブランチ: `claude/fervent-lichterman-ff4d87`

「Apple 審査提出のボタンを押すだけ」「Google Play はできるだけ仕上げる(提出はしない)」を目標に、
ビルド以外を可能な範囲で完了させた。**EAS ビルドは未実施(指示による)。**

---

## 1. 完了済み (コミット済み)

### コード / 審査対応
- Android ログイン不具合の恒久修正 (`app/_layout.tsx`: 未解決ルート時もタブ遷移)
- 未使用依存 `expo-camera` / `expo-blur` を削除 (カメラ権限の無記述リンクという審査スキャン対策)
- コンプライアンス監査: 重大ブロッカーなし。IAP 開示・復元購入・アカウント削除・権限文字列・
  Apple Sign In・ATT 不要(IDFA 非収集)・シークレット非混入・targetSdk・起動安定性 すべて確認済み

### 法規約 (in-app JA/EN + ホスティング HTML)
- 利用規約 / サブスク規約 / 特商法 を **iOS/Android 両対応**に改訂 (課金・解約・返金・無料試用・残高・アップグレード)
- プライバシーポリシーに **PostHog 分析 + セッションリプレイ** の開示を追加
- 改定日を 2026-05-20 に統一
- ⚠️ ホスティング版 `legal/tosche/*.html` は **Cloudflare Pages (todo-phoneapp.pages.dev) への再デプロイ**が必要
  (ストア掲載のプライバシー/規約 URL が反映されるのは再デプロイ後)

### バックエンド (Supabase)
- セキュリティ強化マイグレーション適用済み: `handle_new_user()` の anon/authenticated EXECUTE 取消、
  `set_updated_at()` の search_path 固定
- Edge Functions 10本すべて ACTIVE
- ⏳ **残: 漏洩パスワード保護 (HaveIBeenPwned) の有効化** — Supabase ダッシュボード →
  Authentication → Policies/Settings から手動 ON (任意・審査ブロッカーではない)

### RevenueCat
- iOS/Android/Test の 3 商品が 6 パッケージすべてにアタッチ済み、current オファリング active
- iOS 商品 ID はアプリ・ASC と一致

---

## 2. iOS / App Store Connect — **ビルド以外は完成**

App: ToSche - Life Planner with AI (App ID 6761228359)

| 項目 | 状態 |
| :- | :- |
| バージョン 1.4 作成 | ✅ |
| 「このバージョンの最新情報」EN/JA | ✅ 入力・保存済み |
| スクリーンショット | ✅ 1.2 から継承 |
| App Review デモアカウント・連絡先・メモ | ✅ 設定済み |
| サブスクリプション 6 種 | ✅ 全て「承認済み」 |
| キーワード/URL/著作権/プライバシー URL | ✅ |
| ビルド添付 | ⏳ EAS ビルド後 |
| プライバシー表示に PostHog データ型追加 | ⏳ **1.4 提出時に実施**(現行 1.2 ライブ表示への誤反映を避けるため今は未変更) |

### iOS の「提出ボタンを押すだけ」までの残手順 (あなた)
1. v1.4 機能の追加実装が固まったら `eas build --platform ios --profile production` でビルド → ASC へアップロード
2. ASC の 1.4 → 「ビルド」でアップロードしたビルドを選択
3. 「アプリのプライバシー」→ データ型に **使用状況データ(PostHog: 製品操作)** 等を追加 (1.4 の実態に合わせる)
4. 「審査用に追加」→ 提出

---

## 3. Android / Google Play Console — できるだけ完了

App: ToSche (`com.synthera.tosche`, Play app 4974299383888335999, ステータス「未公開」)

| 項目 | 状態 |
| :- | :- |
| アプリ作成・パッケージ確認・本人確認 | ✅ |
| アプリのコンテンツ (プライバシー/アクセス権/広告/レーティング/対象ユーザー/データセーフティ 他) | ✅ **10/11 完了** |
| ストアの掲載情報テキスト (アプリ名/簡単な説明/詳しい説明 JA) | ✅ 下書き保存済み |
| ストアの掲載情報グラフィック | ⏳ **素材が必要** (下記) |
| サブスクリプション 6 種 | ⛔ **ビルド(AAB)を 1 つアップロードするまで作成不可**(Play 仕様) |
| 製品版リリース (AAB) | ⏳ EAS ビルド後 |

### お支払いアカウント
2026-05-21: **お支払い審査は完了済み**(ユーザー対応済み)。

### ⛔ サブスク作成は「ビルドのアップロード」が前提
定期購入ページの操作は「新しい APK をアップロード」のみで、**AAB を 1 つ Play にアップロードするまで
定期購入を作成できない**(Google Play の仕様)。よってサブスク 6 種は EAS ビルド→アップロード後に作成する。

### ⏳ ストアの掲載情報グラフィック (素材が必要)
- アプリアイコン 512×512 PNG → **`assets/play/play-icon-512.png` に用意済み**(iOS アイコンからリサイズ)。
  ブラウザ自動アップロードはセッションのファイル制限で不可のため、Cowork/手動でアップロードする。
- 特徴グラフィック 1024×500 → **要・新規作成**(Cowork)
- スマートフォンのスクリーンショット 2枚以上 (Play 形式) → **要・新規作成**(Cowork)
> テキストは下書き保存済み。上記グラフィックを追加すれば掲載情報は完成。

### Android サブスク (ビルドアップロード後)
Play Console → 収益化 → 定期購入 で 6 件作成 (商品ID と基本プランIDを厳密一致):
| 商品ID | 基本プランID | 価格 |
| :- | :- | :- |
| `tosche_basic_monthly` | `p1m` | ¥300 |
| `tosche_basic_yearly` | `p1y` | ¥3,240 |
| `tosche_ai_standard_monthly` | `p1m` | ¥1,200 |
| `tosche_ai_standard_yearly` | `p1y` | ¥12,960 |
| `tosche_ai_pro_monthly` | `p1m` | ¥2,000 |
| `tosche_ai_pro_yearly` | `p1y` | ¥21,600 |
作成後、RevenueCat ↔ Google Play の Service Account JSON 連携が必要。

---

## 4. ストア掲載文 (Play 下書き保存済み・参考)

**簡単な説明 (66/80):**
> 目標・ToDo・スケジュール・ルーティンをひとつに。Claude や ChatGPT と連携できる AI搭載のライフプランナー手帳。

**詳しい説明:** Play Console に保存済み(ワークスペース/スケジュール/ルーティン/目標管理/AIアシスタント/外部AI連携/料金 を記載)。

---

## 5. あなたにしかできないこと (まとめ)
1. **Google Play のお支払いアカウント問題の解決** (財務・最優先)
2. **EAS ビルド** (iOS/Android、v1.4 追加実装が固まってから)
3. **Play ストア掲載グラフィック素材**の用意とアップロード
4. **ホスティング版法規約 (legal/tosche/*.html) の Cloudflare Pages 再デプロイ**
5. iOS: ビルド添付 → プライバシー表示更新 → 「審査用に追加」(=提出)
6. (任意) Supabase 漏洩パスワード保護の有効化
